[project]
name = "share-component"
version = "0.1.0"
description = "Share Azure ML Components from workspace to registry"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "azure-ai-ml>=1.28.1",
    "azure-core>=1.35.0",
    "azure-identity>=1.23.1",
    "typer>=0.16.0",
]
